
public class Comparisons {
    /**
     * This method checks if the values of `a` and `b` are equal and returns the result.
     * 
     * 
     * @param a - the first number to be compared.
     * @param b - the second number to be compared.
     * @return - true if a is equal to b, false otherwise.
     */
    public boolean isEqual(int a, int b){
        return false;
    }

    /**
     * This method checks if the value of `a` is greater than the value of `b` and returns the result.
     * 
     * @param a - the first number to be compared.
     * @param b - the second number to be compared.
     * @return - true if a is greater than b, false otherwise.
     */
    public boolean isGreaterThan(int a, int b){
        return false;
    }
    /**
     * This method checks if the value of `a` is less than the value of `b` and returns the result.
     * 
     * @param a - the first number to be compared.
     * @param b - the second number to be compared.
     * @return - true if a is less than b, false otherwise.
     */
    public boolean isLessThan(int a, int b){
        return false;
    }
    /**
     * This method checks if the value of `a` is greater than or equal to the value of `b` and returns the result.
     * 
     * @param a - the first number to be compared.
     * @param b - the second number to be compared.
     * @return - true if a is greater than or equal to b, false otherwise.
     */
    public boolean isGreaterThanOrEqualTo(int a, int b){
        return false;
    }
    /**
     * This method checks if the value of `a` is less than or equal to the value of `b` and returns the result.
     * 
     * @param a - the first number to be compared.
     * @param b - the second number to be compared.
     * @return - true if a is less than or equal to b, false otherwise.
     */
    public boolean isLessThanOrEqualTo(int a, int b){
        return false;
    }

    /**
     * This method checks if the values of `a` and `b` and not equal and returns the result.
     * 
     * @param a - the first number to be compared.
     * @param b - the second number to be compared.
     * @return - true if a is not equal to b, false otherwise.
     */
    public boolean isNotEqualTo(int a, int b){
        return false;
    }
}
